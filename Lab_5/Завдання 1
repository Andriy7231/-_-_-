#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double x, y;

    for (int i = 1; i <= 5; i++) {
        cout << "Введіть x" << i << ": ";
        cin >> x;
        y = sin(pow(x, 5)) + 1.5;
        cout << "y = " << y << endl;
    }

    return 0;
}

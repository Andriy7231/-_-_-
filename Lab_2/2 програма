#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double x, y;
    cout << "Введіть x: ";
    cin >> x;
    cout << "Введіть y: ";
    cin >> y;

    if (x >= 0 && y >= 0 && (xx + yy <= 9)) {
        cout << "Точка належить заштрихованій області";
    } else {
        cout << "Точка НЕ належить заштрихованій області";
    }

    return 0;
}
